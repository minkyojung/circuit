# Browser Mode 기능 - 업데이트된 명세서

> **최종 업데이트**: 2025-01-15
> **기반**: Cursor IDE 브라우저 모드 조사 결과 반영

---

## 📱 전체 화면 레이아웃

### Octave with Browser Mode

```
┌──────────────────────────────────────────────────────────────────────────────┐
│  Octave - duck workspace                                              ○ ○ ○  │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌─────────────────┬─────────────────────────────────┬──────────────────┐   │
│  │                 │                                  │                  │   │
│  │  Left Sidebar   │      Center Content Area         │  Right Sidebar   │   │
│  │  (240px)        │      (Flexible width)            │  (400px)         │   │
│  │                 │                                  │                  │   │
│  │ ┌─────────────┐ │ ┌─────────────────────────────┐ │ ┌──────────────┐ │   │
│  │ │ Repositories│ │ │ Tab Bar                     │ │ │              │ │   │
│  │ ├─────────────┤ │ ├─────────────────────────────┤ │ │              │ │   │
│  │ │ 🏢 octave   │ │ │ [🌐 Browser] [📄 Code] [⚙️] │ │ │  Chat Panel  │ │   │
│  │ └─────────────┘ │ ├─────────────────────────────┤ │ │              │ │   │
│  │                 │ │                             │ │ │  💬 Messages │ │   │
│  │ ┌─────────────┐ │ │   Browser Content           │ │ │              │ │   │
│  │ │ Workspaces  │ │ │   (localhost:3000)          │ │ │  [Type...]   │ │   │
│  │ ├─────────────┤ │ │                             │ │ │              │ │   │
│  │ │ 📁 duck     │ │ ├─────────────────────────────┤ │ ├──────────────┤ │   │
│  │ │ 📁 swan     │ │ │ Inspector/Console (Toggle)  │ │ │              │ │   │
│  │ └─────────────┘ │ │ [Elements][Console][Network]│ │ │  Todo Panel  │ │   │
│  │                 │ └─────────────────────────────┘ │ │              │ │   │
│  │ ┌─────────────┐ │                                  │ │  ✓ Tasks     │ │   │
│  │ │ File Tree   │ │                                  │ │              │ │   │
│  │ ├─────────────┤ │                                  │ └──────────────┘ │   │
│  │ │ 📂 src      │ │                                  │                  │   │
│  │ │ └─ 📄 App   │ │                                  │                  │   │
│  │ └─────────────┘ │                                  │                  │   │
│  └─────────────────┴──────────────────────────────────┴──────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎨 핵심 변경사항

### 1. Center Content Area (중앙 콘텐츠 영역)

**브라우저 탭이 추가됨** (파일 탭과 동일한 위치)

```
탭 구조:
[🌐 localhost:3000] [📄 Button.tsx] [📄 App.tsx] [⚙️ Settings] [+]
     ↑ 브라우저 탭       ↑ 코드 파일 탭들              ↑ 설정
```

**탭 전환:**
- 브라우저 탭 클릭 → 웹 앱 미리보기
- 파일 탭 클릭 → 코드 에디터
- 설정 탭 클릭 → 워크스페이스 설정

---

### 2. Right Sidebar (우측 사이드바) - 기존 구조 유지

**구성**: Chat Panel + Todo Panel (수직 분할)

```
┌──────────────────────────┐
│                          │
│      Chat Panel          │ ← 60% (기본)
│      (상단)               │
│                          │
│  - AI 대화               │
│  - Context Pills         │
│  - Input Area            │
├──────────────────────────┤ ← Resize handle
│                          │
│      Todo Panel          │ ← 40% (기본)
│      (하단)               │
│                          │
│  - Plan 목록             │
│  - Todo 아이템           │
└──────────────────────────┘
```

**기존 Octave 구조를 유지**하여 일관성 확보

---

## 🌐 Browser Tab 상세 스펙

### Browser Tab 구조

브라우저 탭을 선택하면 다음과 같은 레이아웃:

```
┌───────────────────────────────────────────────────────┐
│  Browser Toolbar                                       │
│ ┌─────┬──────────────────────────────┬──────┬────────┐│
│ │ ← → │ http://localhost:3000        │ 🔄  │ 🔍     ││
│ └─────┴──────────────────────────────┴──────┴────────┘│
├───────────────────────────────────────────────────────┤
│                                                        │
│           Browser Content (70%)                        │
│           [Your Web Application]                       │
│           - Fully interactive                          │
│           - Hot reload enabled                         │
│                                                        │
├───────────────────────────────────────────────────────┤
│  Inspector Panel (30% - Toggleable)                    │
│  ┌─────────┬──────────┬──────────┬──────────┐         │
│  │Elements │ Console  │ Network  │ Element  │ ← Tabs  │
│  ├─────────┴──────────┴──────────┴──────────┤         │
│  │                                            │         │
│  │  [Selected content based on active tab]   │         │
│  │                                            │         │
│  └────────────────────────────────────────────┘         │
└───────────────────────────────────────────────────────┘
```

---

### Browser Toolbar 상세

```
┌──┬──┬─────────────────────────────────┬────┬────┬─────┬─────┐
│←│→│  🔒 localhost:3000/dashboard     │ 🔄 │ 🔍│ 🐛 │ ⋮  │
└──┴──┴─────────────────────────────────┴────┴────┴─────┴─────┘
 │  │                                     │    │    │     │
 │  │                                     │    │    │     └─ More menu
 │  │                                     │    │    └─ Console toggle
 │  │                                     │    └─ Inspect mode toggle
 │  │                                     └─ Refresh
 │  └─ Forward
 └─ Back
```

**버튼 설명:**

1. **← → (Back/Forward)**: 브라우저 히스토리 탐색
2. **URL Bar**: 주소 입력 및 표시
3. **🔄 (Refresh)**: 페이지 새로고침
4. **🔍 (Inspect Mode)**: Element 선택 모드 토글
5. **🐛 (Console Toggle)**: 하단 Inspector 패널 토글
6. **⋮ (More)**: DevTools 열기, 스크린샷 등

---

### Inspector Panel - 4가지 탭

#### 1. Elements Tab

```
┌────────────────────────────────────┐
│ HTML Structure                     │
├────────────────────────────────────┤
│ <div id="app">                     │
│   <header>                         │
│     <nav>                          │
│       <button class="submit">  ← 선택│
│         Submit                     │
│       </button>                    │
│     </nav>                         │
│   </header>                        │
│ </div>                             │
├────────────────────────────────────┤
│ Styles                             │
│ .submit {                          │
│   background: #3b82f6;             │
│   padding: 12px 24px;              │
│ }                                  │
└────────────────────────────────────┘
```

#### 2. Console Tab

```
┌────────────────────────────────────┐
│ Console Logs                       │
├────────────────────────────────────┤
│ ⓘ User clicked button              │
│ ⚠ React Hook useEffect missing dep│
│ ❌ Cannot read property 'name'     │
│    at App.tsx:42                   │
│                                    │
│ > _                               │
└────────────────────────────────────┘
```

#### 3. Network Tab

```
┌────────────────────────────────────┐
│ Network Requests                   │
├────────────────────────────────────┤
│ GET /api/users       200  125ms    │
│ POST /api/login      401  89ms     │
│ GET /styles.css      200  12ms     │
│                                    │
└────────────────────────────────────┘
```

#### 4. Element Tab (새 기능!)

```
┌────────────────────────────────────┐
│ 📸 Preview                         │
│ ┌────────────────────────────────┐ │
│ │ [Button Screenshot]            │ │
│ └────────────────────────────────┘ │
│                                    │
│ 📍 Selector                        │
│ button.submit                 [Copy]│
│                                    │
│ 📄 HTML                       [▼]  │
│ <button class="submit">Submit</button>│
│                                    │
│ 🎨 Styles                     [▼]  │
│ background: #3b82f6                │
│ padding: 12px 24px                 │
│                                    │
│ [Add to Chat]                      │
└────────────────────────────────────┘
```

---

## 💬 Chat Panel 업데이트 - 콘솔로그 Context 주입

### Chat Input Area 새 디자인

```
┌─────────────────────────────────────────────────────┐
│ Chat Input                                          │
├─────────────────────────────────────────────────────┤
│ 📎 Context:                                         │
│ [📸 button.submit] [📄 App.tsx L:42-58]    [X]     │
├─────────────────────────────────────────────────────┤
│                                                     │
│  [Plan Mode ▼]  [📄]  [🖼️]  [🐛 Console Logs]      │
│                               ↑ 새로 추가!          │
│  Type your message...                               │
│                                                     │
│  [Send ⌘↵]                                          │
└─────────────────────────────────────────────────────┘
```

### 🐛 Console Logs 버튼 동작

**위치**: Plan Mode 우측

**클릭시 동작:**
1. 현재 브라우저 탭의 **모든 콘솔 로그**를 수집
2. Context Pill로 추가
3. AI에게 전달

**Context Pill 모습:**

```
┌──────────────────────────────────┐
│ 🐛 Console Logs (15 messages)    │
│ [Expand to view]             [X] │
└──────────────────────────────────┘
```

**클릭하면 펼쳐짐:**

```
┌──────────────────────────────────┐
│ 🐛 Console Logs (15 messages)    │
│ ┌──────────────────────────────┐ │
│ │ ❌ Cannot read 'name'        │ │
│ │    App.tsx:42                │ │
│ │                              │ │
│ │ ⚠ Missing dependency         │ │
│ │    useEffect                 │ │
│ │                              │ │
│ │ ⓘ User clicked button        │ │
│ │                              │ │
│ │ ... (12 more)                │ │
│ └──────────────────────────────┘ │
│                              [X] │
└──────────────────────────────────┘
```

### AI가 받는 데이터 형식

```json
{
  "type": "console-logs",
  "source": "browser-tab",
  "url": "http://localhost:3000/dashboard",
  "timestamp": 1705320450000,
  "logs": [
    {
      "level": "error",
      "message": "Cannot read property 'name' of undefined",
      "stackTrace": "at App.tsx:42:15",
      "timestamp": 1705320445123
    },
    {
      "level": "warn",
      "message": "React Hook useEffect has a missing dependency: 'user'",
      "line": 28,
      "file": "Dashboard.tsx"
    },
    {
      "level": "log",
      "message": "User clicked submit button",
      "timestamp": 1705320448900
    }
  ],
  "summary": {
    "total": 15,
    "errors": 3,
    "warnings": 5,
    "info": 7
  }
}
```

---

## 🎯 Inspect Mode - Element Selection Workflow

### 1단계: Inspect Mode 활성화

```
사용자 액션: Toolbar의 🔍 버튼 클릭

화면 변화:
┌─────┬────────────────────┬────┬────┬─────┬────┐
│ ← → │ localhost:3000     │ 🔄 │🔍💙│ 🐛 │ ⋮ │
└─────┴────────────────────┴────┴────┴─────┴────┘
                                   ↑ 파란색으로 강조

┌─────────────────────────────────────────┐
│ 🔍 Inspect Mode Active - Click element │ ← Banner 표시
└─────────────────────────────────────────┘

마우스 커서: 🎯 (타겟 모양으로 변경)
```

### 2단계: Element Hover

```
사용자 액션: 버튼 위에 마우스 올림

화면 변화:
┌────────────────────────────────────┐
│                                    │
│  ┏━━━━━━━━━━━━━━━━━━┓              │
│  ┃  Submit Button   ┃ ← 파란 테두리│
│  ┗━━━━━━━━━━━━━━━━━━┛              │
│  ↑ Box Model Overlay               │
│                                    │
│  Tooltip (버튼 위 floating):       │
│  ┌──────────────────────────┐     │
│  │ button.submit            │     │
│  │ 120px × 48px             │     │
│  │ padding: 12px 24px       │     │
│  └──────────────────────────┘     │
└────────────────────────────────────┘
```

### 3단계: Element Click (선택)

```
사용자 액션: 버튼 클릭

화면 변화:
1. Inspect Mode 자동 비활성화
2. Inspector Panel의 "Element" 탭으로 자동 전환
3. 선택된 요소 정보 표시

Inspector Panel - Element Tab:
┌────────────────────────────────────┐
│ 📸 Preview                         │
│ ┌────────────────────────────────┐ │
│ │ [Submit 버튼 스크린샷]          │ │
│ └────────────────────────────────┘ │
│                                    │
│ 📍 Selector                        │
│ button.submit                 [Copy]│
│                                    │
│ 📄 HTML                            │
│ <button class="submit">Submit</button>│
│                                    │
│ 🎨 Key Styles                      │
│ background: #3b82f6                │
│ color: #ffffff                     │
│ padding: 12px 24px                 │
│                                    │
│ [Add to Chat]  ← 핵심 버튼         │
└────────────────────────────────────┘
```

### 4단계: Add to Chat

```
사용자 액션: [Add to Chat] 버튼 클릭

Chat Panel 변화:
┌────────────────────────────────────┐
│ 📎 Context:                        │
│ ┌──────────────────────────────┐   │
│ │ 📸 button.submit             │   │
│ │ [Mini Preview]               │   │
│ │                          [X] │   │
│ └──────────────────────────────┘   │
├────────────────────────────────────┤
│ Type your message...               │ ← 자동 포커스
└────────────────────────────────────┘

사용자 입력:
"이 버튼을 초록색으로 바꾸고 좀 더 크게 만들어줘"

AI 응답:
"Button.tsx 파일을 수정했습니다.
- backgroundColor: #10b981 (초록색)
- padding: 16px 32px (더 큼)
- fontSize: 18px"

브라우저 자동 새로고침 → 변경사항 확인!
```

---

## 🖥️ 지원 플랫폼 (Cursor 조사 결과 반영)

### ✅ 완벽 지원: 웹 개발

| 프레임워크 | Dev Server | Hot Reload | Element Inspect | 비고 |
|-----------|------------|-----------|-----------------|------|
| **React (Vite)** | localhost:5173 | ✅ | ✅ | Cursor 공식 지원 |
| **Next.js** | localhost:3000 | ✅ | ✅ | Cursor 공식 지원 |
| **Vue** | localhost:8080 | ✅ | ✅ | 완전 호환 |
| **Angular** | localhost:4200 | ✅ | ✅ | 완전 호환 |
| **Svelte** | localhost:5000 | ✅ | ✅ | Vite 기반 |

**기술 스택:**
- Chromium 기반 WebView (BrowserView 또는 WebContentsView)
- Chrome DevTools Protocol (CDP) 통합
- Modern HTML5, CSS3, ES Modules 지원

---

### ⚠️ 제한적 지원

#### **React Native**
- **지원 방식:** 별도 확장 프로그램 필요 ([Radon IDE](https://github.com/software-mansion/radon-ide))
- **기능:**
  - iOS Simulator를 Octave 안에서 실행
  - Android Emulator를 Octave 안에서 실행
- **제약:**
  - 확장 프로그램 설치 필요
  - 시뮬레이터 성능 제한

#### **Expo (React Native Web)**
- **지원:** ✅ 가능
- **방법:** `expo start --web` → localhost:19006 미리보기
- **장점:** 웹으로 컴파일되므로 완벽 호환

#### **Electron**
- **Renderer Process (웹 UI):** ✅ 지원
  - `npm run dev` → localhost:9080 미리보기 가능
- **Main Process (Node.js):** ❌ 불가능
  - Native API, 파일 시스템은 실제 앱 실행 필요

**권장 워크플로우:**
```
Octave Browser: UI 레이아웃, 스타일링, React 컴포넌트
실제 Electron 실행: Native API, 파일 시스템, 창 관리
```

#### **Flutter Web**
- **지원 추정:** ✅ 가능
- **방법:** `flutter run -d web-server` → localhost 미리보기
- **제약:**
  - Canvas Rendering 호환성 문제 가능
  - 공식 검증 필요

#### **Tauri**
- **지원:** Electron과 동일 (웹 부분만)
- **제약:** Rust Native 부분은 실제 앱 실행 필요

---

### ❌ 미지원: 네이티브 모바일

| 플랫폼 | 지원 여부 | 이유 |
|--------|----------|------|
| **iOS (Swift/SwiftUI)** | ❌ | Xcode Simulator 필요 |
| **Android (Kotlin)** | ❌ | Android Emulator 필요 |
| **NativeScript** | ❌ | 네이티브 UI 렌더링만 지원 |

---

### 🔬 Chromium WebView 기술적 제약

**Cursor 공식 문서에 따르면:**

**작동하는 것:**
- Modern HTML5, CSS3, ES Modules
- React, Next.js Dev Servers
- 기본 DevTools (Elements, Console, Network)

**제한적인 것:**
- **Service Workers**: 불안정
- **WebAuthn**: 제한적
- **Push Notifications**: 제한적
- **Hardware-backed credentials**: 제한적

**Cursor 공식 권장사항:**
> "Use Cursor for quick checks, but validate critical auth, caching, and device features in Chrome or Safari."

**Octave의 방향성:**
- **빠른 개발 및 디버깅용**으로 설계
- 프로덕션 검증은 실제 브라우저에서 수행
- Hot Reload 중심의 빠른 반복 개발 지원

---

## 🎬 사용자 워크플로우 - 전체 시나리오

### 시나리오 1: 버튼 색상 변경

```
1. 프로젝트 열기
   → Dev Server 자동 감지 (localhost:3000)
   → 브라우저 탭 자동 생성 (선택적)

2. 브라우저 탭 클릭
   → 웹 앱이 표시됨

3. 🔍 Inspect 버튼 클릭
   → Inspect Mode 활성화

4. 버튼 위에 마우스 올림
   → 파란 테두리 + Tooltip 표시

5. 버튼 클릭
   → Inspector Panel - Element 탭에 정보 표시

6. [Add to Chat] 클릭
   → Chat Panel에 Context Pill 추가

7. "이 버튼 초록색으로 바꿔줘" 입력
   → AI가 Button.tsx 수정
   → 브라우저 자동 새로고침
   → 버튼이 초록색으로 변경!

완료! (창 전환 0회, 소요 시간: 1분)
```

### 시나리오 2: 에러 디버깅

```
1. 브라우저 탭에서 버튼 클릭
   → 에러 발생!

2. Inspector Panel - Console 탭 확인
   → "Cannot read property 'name' of undefined"

3. Chat Input의 🐛 Console Logs 버튼 클릭
   → 모든 콘솔 로그가 Context로 추가됨

4. "이 에러 고쳐줘" 입력
   → AI가 로그 분석
   → App.tsx 42번 라인 수정
   → user 객체 null 체크 추가

5. 브라우저 자동 새로고침
   → 에러 해결!

완료! (Console 복붙 0회, 소요 시간: 30초)
```

### 시나리오 3: 레이아웃 조정

```
1. 브라우저에서 두 버튼이 너무 붙어있음 확인

2. Inspect Mode로 첫 번째 버튼 클릭
   → [Add to Chat]

3. 두 번째 버튼도 클릭
   → [Add to Chat]

4. Chat에 두 개의 Element Pill 표시됨

5. "이 두 버튼 사이에 16px 간격 넣어줘" 입력
   → AI가 첫 번째 버튼에 margin-right 추가
   → 브라우저 자동 새로고침
   → 간격 생김!

완료!
```

---

## 🚀 구현 로드맵

### Phase 1: 기본 브라우저 탭 (2-3일)

**목표:** localhost dev server를 탭으로 미리보기

**작업:**
- Browser Tab 타입 추가 (`editor.ts`)
- BrowserPanel 컴포넌트 생성
- BrowserManager (Main Process) 구현
- WebContentsView 통합

**검증:**
- [ ] localhost:3000 미리보기 가능
- [ ] Hot Reload 작동
- [ ] Tab 전환 (코드 ↔ 브라우저)

---

### Phase 2: Inspector Panel 통합 (2일)

**목표:** 브라우저 탭 하단에 DevTools 패널

**작업:**
- Inspector Panel 컴포넌트 생성
- 4가지 탭 구현 (Elements, Console, Network, Element)
- CDP 통합 (Console 로그 캡처)
- Resizable Panel (브라우저 70% + Inspector 30%)

**검증:**
- [ ] Console 로그 실시간 표시
- [ ] Elements 탭에서 HTML 구조 확인
- [ ] Network 탭에서 요청 확인

---

### Phase 3: Element Inspection (3-4일)

**목표:** Element 선택 및 정보 추출

**작업:**
- CDP Overlay.setInspectMode() 구현
- Element 데이터 추출 (HTML, CSS, Selector)
- Box Model 시각화
- Screenshot 캡처

**검증:**
- [ ] Inspect Mode로 요소 선택 가능
- [ ] Element 탭에 정보 표시
- [ ] [Add to Chat] 버튼 작동

---

### Phase 4: Chat 통합 (2일)

**목표:** Element & Console을 Chat Context로 추가

**작업:**
- Context Pill 컴포넌트 생성
- 🐛 Console Logs 버튼 추가 (Plan Mode 우측)
- AI 메시지 포맷 확장 (Element & Console 데이터)

**검증:**
- [ ] Element Context Pill 추가 가능
- [ ] Console Logs 버튼으로 로그 주입 가능
- [ ] AI가 Element 정보 이해하고 코드 생성

---

### Phase 5: Dev Server 자동 감지 (1일)

**목표:** package.json 파싱하여 자동으로 브라우저 탭 생성

**작업:**
- package.json scripts 파싱
- 포트 번호 추출
- localhost 연결 확인
- 브라우저 탭 자동 생성

**검증:**
- [ ] Vite 프로젝트 → localhost:5173 자동 감지
- [ ] Next.js 프로젝트 → localhost:3000 자동 감지

---

### Phase 6: 안정화 & 최적화 (2일)

**목표:** 에러 처리, 성능 최적화

**작업:**
- BrowserView crash recovery
- Console 로그 필터링
- Screenshot 압축
- 메모리 누수 방지

---

## 📊 성공 지표

### 개발자 경험 개선

**Before (기존 방식):**
- 창: Chrome + VS Code + AI Chat = 3개
- 단계: 버튼 색 바꾸기 = 10단계
- 시간: 약 5분

**After (Octave Browser Mode):**
- 창: Octave 1개
- 단계: 3단계 (Inspect → Add to Chat → AI 요청)
- 시간: 약 30초

**개선율: 90% 시간 단축**

---

## 🔒 제약사항 및 한계

### 기술적 제약

1. **고급 Web API 제한**
   - Service Workers 불안정
   - WebAuthn 제한적
   - Push Notifications 제한적

2. **Native API 미지원**
   - Electron Main Process 테스트 불가
   - iOS/Android Native 앱 미지원

3. **성능**
   - 복잡한 3D/Canvas 앱은 실제 브라우저 권장
   - 대규모 Network 요청은 실제 브라우저에서 검증

### 권장 사용 패턴

**Octave Browser Mode 사용:**
- UI 레이아웃 조정
- 스타일링 수정
- 간단한 디버깅
- 빠른 반복 개발

**실제 브라우저 사용:**
- 인증/권한 테스트
- Service Worker 테스트
- 프로덕션 검증
- 성능 프로파일링

---

## 🎯 핵심 가치 제안

### 1. Zero Context Switch
- 모든 것이 한 화면 (코드 + 브라우저 + AI + Todo)
- 창 전환 불필요
- 집중력 유지

### 2. Visual-First Workflow
- 보이는 것을 클릭 → 정확한 정보 자동 추출
- 복붙 불필요
- 스크린샷 자동 캡처

### 3. AI-Powered Intelligence
- 정확한 Element Context (Selector, HTML, CSS)
- Console 로그 자동 주입
- 즉각적인 코드 생성 및 적용

### 4. Cursor-Compatible
- Cursor IDE와 동일한 플랫폼 지원
- 동일한 기술적 제약사항
- 프로덕션 검증 가이드라인 일치

---

## 📝 요약

**이 기능은 무엇인가?**
> Octave 안에 Chromium 브라우저를 통합하고, Element Inspection + Console Logs를 한 번의 클릭으로 AI에게 전달하여 즉각적인 코드 수정을 가능하게 하는 기능

**왜 필요한가?**
> 창 전환, 복붙, 파일 찾기 등의 번거로움을 제거하고, UI 디버깅 및 수정을 몇 초 만에 완료하기 위해

**어떻게 만드나?**
> 6단계 (기본 브라우저 탭 → Inspector Panel → Element Inspection → Chat 통합 → 자동 감지 → 안정화)

**언제 완성되나?**
> 약 2-3주 (총 12-16일)

**누가 사용하나?**
> 웹 개발자, 특히 UI/UX를 자주 수정하는 프론트엔드 개발자

**왜 혁신적인가?**
> "보고 → 클릭하고 → AI가 고치는" 완전한 비주얼 워크플로우 + Console 로그 자동 주입

---

궁금한 점이나 추가 설명이 필요하면 언제든지 물어보세요!
