# Browser Tab 기능 - 최종 사용자 경험 명세서

## 📱 전체 화면 레이아웃

### 개발 완료 후 Octave의 모습

```
┌──────────────────────────────────────────────────────────────────────────────┐
│  Octave - 파일명: Button.tsx                                          ○ ○ ○  │ macOS Title Bar
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌─────────────────┬─────────────────────────────────┬──────────────────┐   │
│  │                 │                                  │                  │   │
│  │  Left Sidebar   │      Center Content Area         │  Right Sidebar   │   │
│  │  (240px)        │      (Flexible width)            │  (400px)         │   │
│  │                 │                                  │                  │   │
│  │ ┌─────────────┐ │ ┌─────────────────────────────┐ │ ┌──────────────┐ │   │
│  │ │ Repositories│ │ │ Tab Bar                     │ │ │              │ │   │
│  │ ├─────────────┤ │ ├─────────────────────────────┤ │ │              │ │   │
│  │ │ 🏢 octave   │ │ │ [Browser] [Button.tsx] ⚙️  │ │ │  Chat Panel  │ │   │
│  │ │ 🏢 project  │ │ ├─────────────────────────────┤ │ │              │ │   │
│  │ └─────────────┘ │ │                             │ │ │  💬 Messages │ │   │
│  │                 │ │                             │ │ │              │ │   │
│  │ ┌─────────────┐ │ │   Browser Preview           │ │ │  [Type...]   │ │   │
│  │ │ Workspaces  │ │ │   or                        │ │ │              │ │   │
│  │ ├─────────────┤ │ │   File Editor               │ │ ├──────────────┤ │   │
│  │ │ 📁 duck     │ │ │   or                        │ │ │              │ │   │
│  │ │ 📁 swan     │ │ │   Settings Panel            │ │ │  Inspector   │ │   │
│  │ │ 📁 goose    │ │ │                             │ │ │              │ │   │
│  │ └─────────────┘ │ │                             │ │ │  [Element]   │ │   │
│  │                 │ │                             │ │ │  [Info]      │ │   │
│  │ ┌─────────────┐ │ │                             │ │ │              │ │   │
│  │ │ File Tree   │ │ │                             │ │ └──────────────┘ │   │
│  │ ├─────────────┤ │ │                             │ │                  │   │
│  │ │ 📂 src      │ │ │                             │ │                  │   │
│  │ │ └─ 📄 App   │ │ │                             │ │                  │   │
│  │ │ 📂 styles   │ │ └─────────────────────────────┘ │                  │   │
│  │ └─────────────┘ │                                  │                  │   │
│  └─────────────────┴──────────────────────────────────┴──────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎨 화면 영역별 상세 분석

### 1. Left Sidebar (좌측 사이드바) - 변화 없음

**크기**: 240px (collapsible)

**구성**:
```
┌─────────────────────┐
│  Repository Switcher │
│  🏢 octave          ▼│
├─────────────────────┤
│  Workspaces         │
│  📁 duck (main)      │ ← 현재 선택된 workspace
│  📁 swan (feat/ui)   │
│  📁 goose (hotfix)   │
│  [+ New Workspace]   │
├─────────────────────┤
│  File Explorer      │
│  📂 src             │
│  ├─ 📂 components   │
│  │  └─ 📄 Button.tsx│ ← 현재 열린 파일
│  ├─ 📂 styles       │
│  └─ 📄 App.tsx      │
└─────────────────────┘
```

**기능**: (기존과 동일)
- Repository 전환
- Workspace (branch) 관리
- 파일 탐색 및 열기
- Git 상태 표시

---

### 2. Center Content Area (중앙 콘텐츠) - 핵심 변경 영역

#### 2.1 Tab Bar (탭 바)

**높이**: 36px

**모습**:
```
┌─────────────────────────────────────────────────────────┐
│ [🌐 localhost:3000] [📄 Button.tsx] [⚙️ Settings] [+]  │
│      ↑ 활성화            ↑ 수정됨 (●)      ↑ 설정       │
└─────────────────────────────────────────────────────────┘
```

**탭 종류**:

1. **Browser Tab** (새로 추가!)
   ```
   ┌──────────────────────┐
   │ 🌐 localhost:3000   X│ ← Favicon + URL + Close 버튼
   └──────────────────────┘
   ```
   - **Favicon**: 웹사이트 아이콘 (있으면 표시, 없으면 🌐)
   - **Title**: 페이지 제목 또는 URL
   - **Indicator**: 로딩 중이면 spinner, 에러면 ⚠️
   - **Close**: X 버튼으로 탭 닫기

2. **File Tab** (기존)
   ```
   ┌──────────────────────┐
   │ 📄 Button.tsx ● X   │ ← 파일 아이콘 + 이름 + 수정 표시
   └──────────────────────┘
   ```

3. **Settings Tab** (기존)
   ```
   ┌──────────────────────┐
   │ ⚙️ Settings X       │
   └──────────────────────┘
   ```

**탭 인터랙션**:
- **Click**: 탭 활성화
- **Drag**: 탭 순서 변경, 다른 그룹으로 이동 (split view)
- **Middle Click / Cmd+W**: 탭 닫기
- **Hover**: 전체 경로 툴팁 표시

---

#### 2.2 Browser Panel (브라우저 패널) - 새 기능!

**활성화 조건**: Browser 탭이 선택되었을 때

**구조**:
```
┌───────────────────────────────────────────────────────┐
│  Browser Toolbar                                       │
│ ┌─────┬──────────────────────────────┬──────┬────────┐│
│ │ ← → │ http://localhost:3000        │ 🔄  │ 🔍     ││
│ └─────┴──────────────────────────────┴──────┴────────┘│
├───────────────────────────────────────────────────────┤
│                                                        │
│                                                        │
│           Actual Browser Content                      │
│           (BrowserView renders here)                  │
│                                                        │
│           [Your Web Application]                      │
│           - Buttons, Forms, etc.                      │
│           - Fully interactive                         │
│           - Hot reload enabled                        │
│                                                        │
│                                                        │
└───────────────────────────────────────────────────────┘
```

**Toolbar 상세**:

```
┌──┬──┬─────────────────────────────────┬────┬────┬─────┐
│←│→│  🔒 localhost:3000/dashboard     │ 🔄 │ 🔍│ ⋮  │
└──┴──┴─────────────────────────────────┴────┴────┴─────┘
 │  │                                     │    │     │
 │  │                                     │    │     └─ More menu
 │  │                                     │    └─ Inspect mode toggle
 │  │                                     └─ Refresh
 │  └─ Forward
 └─ Back
```

**버튼 상세**:

1. **← Back** (뒤로)
   - Disabled: 이전 페이지 없을 때 회색
   - Enabled: 클릭하면 이전 페이지
   - Tooltip: "Go back"

2. **→ Forward** (앞으로)
   - Disabled: 다음 페이지 없을 때 회색
   - Enabled: 클릭하면 다음 페이지

3. **URL Bar**
   - **Display Mode**: 현재 URL 표시
   - **Edit Mode**: 클릭하면 전체 선택, 입력 가능
   - **Enter**: 입력한 URL로 이동
   - **Indicator**:
     - 🔒 = HTTPS
     - ⚠️ = HTTP
     - 🌐 = localhost

4. **🔄 Refresh**
   - Click: 새로고침
   - Animation: 로딩 중에는 회전 애니메이션

5. **🔍 Inspect Mode Toggle** ← 핵심 기능!
   - **Off 상태**: 회색, "Enable Inspect Mode"
   - **On 상태**: 파란색, "Disable Inspect Mode"
   - **Visual Feedback**: 켜지면 toolbar 배경이 살짝 파란색으로 변함
   - **Shortcut**: Cmd+Shift+I

6. **⋮ More Menu**
   - Open DevTools (Cmd+Option+I)
   - Reload Without Cache (Cmd+Shift+R)
   - View Page Source
   - Take Screenshot

---

#### 2.3 Browser Panel - Inspect Mode Active (검사 모드)

**Inspect 모드가 켜졌을 때**:

```
┌───────────────────────────────────────────────────────┐
│ ┌─────┬──────────────────────────┬────┬──────┬─────┐ │
│ │ ← → │ localhost:3000           │ 🔄 │ 🔍💙│ ⋮  │ │
│ └─────┴──────────────────────────┴────┴──────┴─────┘ │
│ ┌───────────────────────────────────────────────────┐ │
│ │ 🔍 Inspect Mode Active - Click element to select │ │ ← Banner
│ └───────────────────────────────────────────────────┘ │
├───────────────────────────────────────────────────────┤
│                                                        │
│      [Header Navigation]  ← Normal                    │
│                                                        │
│      ┌──────────────────┐                             │
│      │   Submit Button  │ ← 마우스 올림               │
│      └──────────────────┘                             │
│         ↑                                              │
│    파란 테두리 + 오버레이 나타남!                       │
│                                                        │
│   ┌─────────────────────────────────────┐             │
│   │ button.submit                        │ ← Tooltip  │
│   │ 120px × 48px                         │             │
│   │ margin: 0px  padding: 12px 24px     │             │
│   │ #3b82f6 (blue)                      │             │
│   └─────────────────────────────────────┘             │
│                                                        │
└───────────────────────────────────────────────────────┘
```

**Hover Overlay 시각화**:

실제 요소 위에 반투명 오버레이가 표시됩니다:

```
Element Box Model Overlay:

┌─────────────────────────────────────┐
│ Margin (주황색 반투명)               │
│ ┌─────────────────────────────────┐ │
│ │ Border (노란색 반투명)           │ │
│ │ ┌─────────────────────────────┐ │ │
│ │ │ Padding (초록색 반투명)      │ │ │
│ │ │ ┌─────────────────────────┐ │ │ │
│ │ │ │ Content (파란색 반투명) │ │ │ │
│ │ │ │   Submit Button         │ │ │ │
│ │ │ └─────────────────────────┘ │ │ │
│ │ └─────────────────────────────┘ │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

**색상 코드**:
- Content: rgba(111, 168, 220, 0.66) - 파란색
- Padding: rgba(147, 196, 125, 0.55) - 초록색
- Border: rgba(255, 229, 153, 0.66) - 노란색
- Margin: rgba(246, 178, 107, 0.66) - 주황색

**Tooltip 정보**:
```
┌────────────────────────────┐
│ button.submit              │ ← CSS Selector
│ ────────────────────────   │
│ 📏 120px × 48px            │ ← 크기
│ 📦 margin: 0px             │ ← Margin
│ 📦 padding: 12px 24px      │ ← Padding
│ 🎨 #3b82f6 (blue-600)      │ ← 배경색
│ 🔤 #ffffff (white)         │ ← 글자색
│ 📐 border-radius: 8px      │ ← 둥근 모서리
└────────────────────────────┘
```

---

#### 2.4 File Editor Panel (기존, 변화 없음)

**활성화 조건**: File 탭 선택 시

**모습**:
```
┌───────────────────────────────────────────────────────┐
│  Button.tsx - /src/components/Button.tsx              │
├───────────────────────────────────────────────────────┤
│ 1  import React from 'react'                          │
│ 2                                                      │
│ 3  export function Button({ children, onClick }) {    │
│ 4    return (                                          │
│ 5      <button                                         │
│ 6        className="submit"                            │
│ 7        onClick={onClick}                             │
│ 8      >                                               │
│ 9        {children}                                    │
│10      </button>                                       │
│11    )                                                 │
│12  }                                                   │
│                                                        │
│  [Monaco Editor - Code highlighting, autocomplete]    │
└───────────────────────────────────────────────────────┘
```

---

### 3. Right Sidebar (우측 사이드바) - 핵심 변경!

**크기**: 400px (resizable, 300px ~ 600px)

**새 구조**: 위아래로 분할 (ResizablePanel)

```
┌──────────────────────────┐
│                          │
│      Chat Panel          │ ← 60% (기본)
│      (상단)               │
│                          │
│                          │
├──────────────────────────┤ ← Resize handle
│                          │
│   Element Inspector      │ ← 40% (기본)
│   (하단)                  │
│                          │
└──────────────────────────┘
```

#### 3.1 Chat Panel (상단 패널) - 위치 변경

**이전 위치**: Center area의 tab content
**새 위치**: Right sidebar 상단 (persistent)

**구조**:
```
┌────────────────────────────────────┐
│  💬 Chat - duck (main)              │ ← Header
├────────────────────────────────────┤
│                                    │
│  👤 You: 버튼 색상 바꿔줘           │
│  15:30                             │
│                                    │
│  🤖 Claude:                        │
│  Button.tsx를 수정했습니다.         │
│  - backgroundColor: #10b981       │
│  - 초록색으로 변경되었습니다        │
│  15:30                             │
│                                    │
│  [File: Button.tsx]                │ ← File reference pill
│  [Element: button.submit]          │ ← Element context pill
│                                    │
│  ▼ Show code diff                  │
│                                    │
│  👤 You: 좀 더 진한 색으로           │
│  15:31                             │
│                                    │
│  🤖 Claude: (Typing...)            │
│                                    │
│                                    │
├────────────────────────────────────┤
│ 📎 Context:                        │ ← Context pills (접을 수 있음)
│ [📸 button.submit] [📄 Button.tsx] │
├────────────────────────────────────┤
│ Type a message...                  │ ← Input
│                                    │
│ [Send] [⌘↵]                        │
└────────────────────────────────────┘
```

**주요 기능**:

1. **Message Stream**
   - User messages (우측 정렬)
   - AI responses (좌측 정렬)
   - Timestamp
   - Thinking steps (접을 수 있음)

2. **Context Pills** (새 기능!)
   ```
   ┌──────────────────────────────────┐
   │ 📎 Attached Context:             │
   │ ┌────────────────┐ ┌───────────┐│
   │ │ 📸 button.submit│ │📄 App.tsx ││
   │ │ [Preview img]   │ │ L:42-58  ││
   │ │ [X]            │ │ [X]      ││
   │ └────────────────┘ └───────────┘│
   └──────────────────────────────────┘
   ```

   **Element Pill**:
   - Thumbnail screenshot
   - Selector text
   - Remove button (X)
   - Click to view details

   **File Pill**:
   - File icon
   - File name
   - Line range (if applicable)
   - Remove button

3. **Input Area**
   - Multiline text input
   - Send button
   - Keyboard shortcut indicator (⌘↵)
   - Attachment buttons (향후: 파일, 스크린샷)

---

#### 3.2 Element Inspector Panel (하단 패널) - 새 기능!

**활성화 조건**: Browser 탭이 열려있고, Inspect 모드로 요소 선택했을 때

**Empty State** (아무것도 선택 안 됨):
```
┌────────────────────────────────────┐
│  🔍 Element Inspector               │
├────────────────────────────────────┤
│                                    │
│         🎯                         │
│                                    │
│   Enable Inspect Mode              │
│   and click an element             │
│   to view details                  │
│                                    │
│                                    │
└────────────────────────────────────┘
```

**Active State** (요소 선택됨):
```
┌────────────────────────────────────┐
│  🔍 Element Inspector     [Add to Chat]│ ← Header with action
├────────────────────────────────────┤
│                                    │
│  📸 Preview                        │
│  ┌──────────────────────────────┐ │
│  │  [Button screenshot]         │ │
│  │  Submit Button               │ │
│  └──────────────────────────────┘ │
│                                    │
│  📍 Selector                       │
│  ┌──────────────────────────────┐ │
│  │ button.submit                │ │
│  │                         [Copy]│ │
│  └──────────────────────────────┘ │
│                                    │
│  📄 HTML                      [▼] │ ← Collapsible
│  ┌──────────────────────────────┐ │
│  │ <button class="submit">      │ │
│  │   Submit                     │ │
│  │ </button>                [Copy]│ │
│  └──────────────────────────────┘ │
│                                    │
│  🎨 Styles                    [▼] │
│  ┌──────────────────────────────┐ │
│  │ display: inline-flex         │ │
│  │ background: #3b82f6          │ │
│  │ color: #ffffff               │ │
│  │ padding: 12px 24px           │ │
│  │ border-radius: 8px           │ │
│  │ font-size: 16px              │ │
│  │ ... (50 more)          [Show]│ │
│  └──────────────────────────────┘ │
│                                    │
│  📦 Box Model                 [▼] │
│  ┌──────────────────────────────┐ │
│  │    120 × 48                  │ │
│  │  ┌──────────────────────┐   │ │
│  │  │  margin: 0           │   │ │
│  │  │ ┌──────────────────┐ │   │ │
│  │  │ │ border: 0        │ │   │ │
│  │  │ │ ┌──────────────┐ │ │   │ │
│  │  │ │ │padding: 12×24│ │ │   │ │
│  │  │ │ │┌────────────┐│ │ │   │ │
│  │  │ │ ││content     ││ │ │   │ │
│  │  │ │ ││ 96 × 24    ││ │ │   │ │
│  │  │ │ │└────────────┘│ │ │   │ │
│  │  │ │ └──────────────┘ │ │   │ │
│  │  │ └──────────────────┘ │   │ │
│  │  └──────────────────────┘   │ │
│  └──────────────────────────────┘ │
│                                    │
│  [Add to Chat] [Copy All]          │ ← Actions
└────────────────────────────────────┘
```

**각 섹션 상세**:

1. **Preview Section**
   - Element screenshot (actual captured image)
   - Max width: 100%
   - Border with shadow
   - Click to enlarge (modal)

2. **Selector Section**
   - CSS selector text
   - Monospace font
   - Copy button (클릭 시 클립보드에 복사)
   - Valid indicator (선택자가 유효한지)

3. **HTML Section** (Collapsible)
   - Syntax-highlighted HTML
   - Line numbers
   - Copy button
   - Prettified format

4. **Styles Section** (Collapsible)
   - Computed styles (중요한 것만 기본 표시)
   - Color swatches (색상 옆에 색상 박스)
   - "Show All" button (50+ styles)
   - Filter input (검색)

5. **Box Model Diagram**
   - Interactive visualization
   - Hover on each layer to highlight
   - Actual pixel values
   - Color-coded layers

**Action Buttons**:

1. **[Add to Chat]** (Primary)
   - Blue button
   - Click → Element context added to chat
   - Feedback: "Added to chat ✓"

2. **[Copy All]** (Secondary)
   - Copies entire element data as JSON
   - Useful for debugging

---

## 🔄 사용자 워크플로우 - 상세 시뮬레이션

### Workflow 1: 처음 프로젝트 열기

**단계별 화면 변화**:

#### Step 1: Workspace 선택
```
사용자 액션: Left Sidebar에서 "duck (main)" 클릭

화면 변화:
- Left sidebar의 file tree가 duck workspace의 파일로 업데이트
- Center area에 conversation tab 자동 생성
- Right sidebar의 chat panel이 duck workspace의 대화 표시
```

#### Step 2: Dev Server 자동 감지
```
시스템 동작 (백그라운드):
1. package.json 파일 읽기
   {
     "scripts": {
       "dev": "vite --port 3000"
     }
   }

2. Vite 감지 → Port 3000 확인

3. localhost:3000 접속 시도
   - 성공: Browser tab 자동 생성
   - 실패: Notification 표시

화면 변화:
┌────────────────────────────────────┐
│ Toast Notification (우하단)        │
│ ✅ Dev server detected!            │
│ Opening localhost:3000...          │
└────────────────────────────────────┘

Tab Bar:
[💬 Chat] → [🌐 localhost:3000] [💬 Chat]
             ↑ 새 tab 생성 및 활성화
```

#### Step 3: Browser Tab 열림
```
Center Area:
┌───────────────────────────────────────┐
│ Tab: [🌐 localhost:3000]              │
├───────────────────────────────────────┤
│ Toolbar:                              │
│ [← →] [http://localhost:3000] [🔄 🔍]│
├───────────────────────────────────────┤
│                                       │
│  Loading...  (Spinner animation)      │
│                                       │
└───────────────────────────────────────┘

2초 후:
┌───────────────────────────────────────┐
│ [실제 웹사이트가 렌더링됨]             │
│                                       │
│  [Dashboard Header]                   │
│  [Navigation Menu]                    │
│  [Content Area]                       │
│  [Footer]                             │
│                                       │
└───────────────────────────────────────┘
```

---

### Workflow 2: Element Inspection & Chat 통합

#### Step 1: Inspect 모드 활성화
```
사용자 액션: Toolbar에서 🔍 버튼 클릭

화면 변화:

Before:
┌─────┬────────────────────┬────┬────┬───┐
│ ← → │ localhost:3000     │ 🔄 │ 🔍│ ⋮│
└─────┴────────────────────┴────┴────┴───┘

After:
┌─────┬────────────────────┬────┬────┬───┐
│ ← → │ localhost:3000     │ 🔄 │🔍💙│ ⋮│ ← 파란색 강조
└─────┴────────────────────┴────┴────┴───┘
┌─────────────────────────────────────────┐
│ 🔍 Inspect Mode Active - Click element │ ← Banner 나타남
└─────────────────────────────────────────┘

마우스 커서: 기본 → 🎯 (타겟 커서)
```

#### Step 2: Element Hover
```
사용자 액션: Submit 버튼 위에 마우스 올림

화면 변화 (0.05초 이내):

Browser Content:
┌────────────────────────────────────┐
│                                    │
│  [Header]                          │
│                                    │
│  ┏━━━━━━━━━━━━━━━━━━┓              │
│  ┃  Submit Button   ┃ ← 파란 테두리│
│  ┗━━━━━━━━━━━━━━━━━━┛              │
│  ↑                                 │
│  Box Model Overlay (반투명 레이어) │
│  - Content: 파란색                 │
│  - Padding: 초록색 (12px 24px)    │
│  - Border: 없음                    │
│  - Margin: 없음                    │
│                                    │
│  Tooltip (버튼 위 floating):       │
│  ┌──────────────────────────┐     │
│  │ button.submit            │     │
│  │ 120px × 48px             │     │
│  │ padding: 12px 24px       │     │
│  │ #3b82f6                  │     │
│  └──────────────────────────┘     │
└────────────────────────────────────┘
```

#### Step 3: Element Click (선택)
```
사용자 액션: 버튼 클릭

화면 변화 (즉시):

1. Inspect Mode 자동 비활성화
   - Banner 사라짐
   - 🔍 버튼 다시 회색으로

2. Right Sidebar - Element Inspector 업데이트

Before (Empty State):
┌────────────────────────────────────┐
│ 🔍 Element Inspector               │
│                                    │
│ Enable Inspect Mode...             │
└────────────────────────────────────┘

After (Element Selected):
┌────────────────────────────────────┐
│ 🔍 Element Inspector  [Add to Chat]│
├────────────────────────────────────┤
│ 📸 Preview                         │
│ ┌────────────────────────────────┐ │
│ │ [버튼 실제 스크린샷]            │ │
│ │  Submit                        │ │
│ └────────────────────────────────┘ │
│                                    │
│ 📍 Selector                        │
│ button.submit                 [Copy]│
│                                    │
│ 📄 HTML                       [▼]  │
│ <button class="submit">...         │
│                                    │
│ 🎨 Styles                     [▼]  │
│ background-color: #3b82f6          │
│ ...                                │
│                                    │
│ [Add to Chat]                      │
└────────────────────────────────────┘

3. Toast Notification:
┌────────────────────────────────────┐
│ ✓ Element selected                 │
│ button.submit                      │
└────────────────────────────────────┘
```

#### Step 4: Add to Chat
```
사용자 액션: [Add to Chat] 버튼 클릭

화면 변화:

Right Sidebar - Chat Panel:

Before:
├────────────────────────────────────┤
│ Type a message...                  │
│ [Send]                             │
└────────────────────────────────────┘

After:
├────────────────────────────────────┤
│ 📎 Context:                        │
│ ┌──────────────────┐               │
│ │ 📸 button.submit │               │
│ │ [Mini preview]   │               │
│ │ [X]             │               │
│ └──────────────────┘               │
├────────────────────────────────────┤
│ Type a message...                  │ ← Focus 자동 이동
│ [Send]                             │
└────────────────────────────────────┘

Feedback:
- Element pill appears with animation (slide up)
- Input field gets focus
- Placeholder text: "Ask about this element..."
```

#### Step 5: AI에게 요청
```
사용자 액션:
"이 버튼을 초록색으로 바꾸고 좀 더 크게 만들어줘" 입력 후 Send

화면 변화:

Chat Panel:
┌────────────────────────────────────┐
│ 👤 You: 이 버튼을 초록색으로 바꾸고│
│ 좀 더 크게 만들어줘                │
│                                    │
│ 📎 Context:                        │
│ [📸 button.submit]                 │
│                                    │
│ 16:45                              │
├────────────────────────────────────┤
│ 🤖 Claude: (Typing...)             │ ← 타이핑 인디케이터
└────────────────────────────────────┘

5초 후:
┌────────────────────────────────────┐
│ 🤖 Claude:                         │
│ Button.tsx 파일을 수정했습니다.     │
│                                    │
│ 변경사항:                          │
│ ✓ 배경색: #10b981 (초록색)         │
│ ✓ Padding: 16px 32px (더 큼)      │
│ ✓ Font size: 18px                 │
│                                    │
│ ▼ Show code diff                   │
│                                    │
│ [📄 Button.tsx] ← 파일 pill       │
│ 16:45                              │
└────────────────────────────────────┘

Center Area (자동):
- Button.tsx 탭이 자동으로 열림 (또는 활성화)
- 해당 라인으로 스크롤
- 변경된 코드 하이라이트

Browser Tab (자동):
- Hot reload 감지
- 페이지 자동 새로고침
- 버튼이 초록색으로, 더 크게 변함!
```

---

### Workflow 3: 여러 요소 비교

#### Scenario: 두 버튼의 간격 조정

```
Step 1: 첫 번째 버튼 선택
- Inspect 모드 켜기
- 버튼 A 클릭
- [Add to Chat]

Step 2: 두 번째 버튼 선택
- Inspect 모드 다시 켜기 (자동으로 꺼졌으므로)
- 버튼 B 클릭
- [Add to Chat]

Chat Panel 상태:
┌────────────────────────────────────┐
│ 📎 Context:                        │
│ ┌──────────┐ ┌──────────┐         │
│ │📸 .btn-a │ │📸 .btn-b │         │
│ │[Preview] │ │[Preview] │         │
│ │   [X]    │ │   [X]    │         │
│ └──────────┘ └──────────┘         │
└────────────────────────────────────┘

사용자 입력:
"이 두 버튼 사이 간격을 좀 벌려줘"

AI 응답:
"두 버튼 사이에 16px 간격을 추가했습니다.
.btn-a에 margin-right: 16px 적용했습니다."

결과:
- 코드 자동 수정
- 브라우저 자동 새로고침
- 간격 벌어짐 확인
```

---

## 🎯 UX 세부사항 - 마이크로 인터랙션

### 1. Hover States (마우스 오버 효과)

**Tab Hover**:
```
Normal:
[🌐 localhost:3000]

Hover:
┌──────────────────────────────┐
│ 🌐 localhost:3000            │ ← 배경 살짝 밝아짐
│                          [X] │ ← Close 버튼 나타남
└──────────────────────────────┘
Tooltip: "http://localhost:3000/dashboard"
```

**Button Hover**:
```
🔍 Inspect Button:

Normal: 🔍 (회색)
Hover:  🔍 (밝은 회색, 배경 원형)
Active: 🔍💙 (파란색)
```

### 2. Loading States (로딩 상태)

**Page Loading**:
```
┌────────────────────────────────────┐
│ [← →] [localhost:3000] [⟳] [🔍]   │ ← ⟳ 회전 애니메이션
└────────────────────────────────────┘

Content Area:
┌────────────────────────────────────┐
│                                    │
│         ⟳ Loading...               │
│     (Spinner animation)            │
│                                    │
└────────────────────────────────────┘
```

**AI Response Loading**:
```
Chat:
🤖 Claude: (Typing...)
           ⋯⋯⋯ ← 점 3개가 좌우로 움직임
```

### 3. Transitions & Animations

**Element Inspector 열릴 때**:
```
Animation: Slide up + Fade in
Duration: 200ms
Easing: ease-out

Content가 아래에서 위로 슬라이드하며 나타남
```

**Context Pill 추가될 때**:
```
Animation: Scale up + Fade in
Duration: 150ms
Easing: cubic-bezier(0.34, 1.56, 0.64, 1) (bounce)

작은 크기에서 튀어나오듯 나타남
```

**Tab 전환**:
```
Animation: Cross-fade
Duration: 100ms

이전 content fade out → 새 content fade in
```

### 4. Feedback (피드백)

**Success Feedback**:
```
Toast Notification:
┌────────────────────────────────────┐
│ ✓ Element added to chat            │ ← 초록색 배경
└────────────────────────────────────┘
위치: 우하단
Duration: 2초
Animation: Slide in from right
```

**Error Feedback**:
```
Toast:
┌────────────────────────────────────┐
│ ⚠️ Dev server not running           │ ← 빨간색 배경
│ Start server with: npm run dev     │
└────────────────────────────────────┘
Duration: 5초
```

**Copy Feedback**:
```
Button State 변화:
[Copy] → [Copied ✓] (1초간) → [Copy]

색상: Gray → Green → Gray
```

---

## 📊 Performance & Optimization

### 예상 성능 지표

**BrowserView 생성**:
- Cold start: ~500ms
- Warm start: ~200ms
- User feedback: Progress indicator

**Element Selection**:
- Hover highlight: <50ms (즉각 반응)
- Click → Data extraction: <200ms
- Screenshot capture: <300ms

**AI Response**:
- Simple request: 2-5초
- Complex request: 5-10초
- Streaming: 실시간 (타이핑 효과)

**Browser Reload**:
- Hot reload: <1초 (Vite)
- Hard reload: 2-3초

---

## 🎨 테마 & 스타일링

### Color Palette

**Inspect Mode Highlights**:
- Content: `rgba(111, 168, 220, 0.66)` - 파란색
- Padding: `rgba(147, 196, 125, 0.55)` - 초록색
- Border: `rgba(255, 229, 153, 0.66)` - 노란색
- Margin: `rgba(246, 178, 107, 0.66)` - 주황색

**UI Elements**:
- Primary Action: `#3b82f6` (blue-600)
- Success: `#10b981` (green-600)
- Warning: `#f59e0b` (amber-600)
- Error: `#ef4444` (red-600)
- Muted: `#6b7280` (gray-600)

### Typography

**Monospace** (코드, 선택자):
- Font: JetBrains Mono
- Size: 14px
- Line height: 1.5

**UI Text**:
- Font: SF Pro Display
- Size: 14px (body), 12px (small)
- Line height: 1.4

---

## 🔒 안전장치 & 에러 처리

### Dev Server Not Running

```
Browser Tab:
┌────────────────────────────────────┐
│ ⚠️ Connection Failed                │
│                                    │
│ Could not connect to:              │
│ http://localhost:3000              │
│                                    │
│ Start your dev server:             │
│ $ npm run dev                      │
│                                    │
│ [Retry] [Change URL]               │
└────────────────────────────────────┘
```

### Invalid URL

```
URL Bar:
┌──────────────────────────────────┐
│ httpp://invalid-url          ⚠️ │ ← 빨간 경고
└──────────────────────────────────┘

Error Message (아래):
"Invalid URL format"
```

### BrowserView Crash

```
Automatic Recovery:
1. Detect crash
2. Show error overlay:
   ┌────────────────────────────────┐
   │ ⚠️ Browser crashed              │
   │ [Reload] [Close Tab]           │
   └────────────────────────────────┘
3. User clicks [Reload]
4. BrowserView recreates
5. Restores last URL
```

---

## 🎯 최종 사용자 경험 요약

### 개발자의 하루 (Before vs After)

**Before (기존 방식)**:
```
8:00 - 프로젝트 열기
8:01 - Terminal 열어서 npm run dev
8:02 - Chrome 열기
8:03 - localhost:3000 입력
8:04 - F12로 DevTools 열기
8:05 - 버튼 inspect
8:06 - CSS 복사
8:07 - VS Code로 전환
8:08 - AI 채팅 열기
8:09 - "이 버튼..." + CSS 붙여넣기
8:10 - AI 응답 기다림
8:11 - 코드 복사
8:12 - 파일 찾기
8:13 - 코드 수정
8:14 - 저장
8:15 - Chrome으로 전환
8:16 - 새로고침
8:17 - 확인
---
총 17분, 10번의 창 전환
```

**After (새 방식)**:
```
8:00 - 프로젝트 열기
     → Browser tab 자동 생성
     → Dev server 자동 감지
8:01 - 🔍 클릭
8:02 - 버튼 클릭
8:03 - [Add to Chat]
8:04 - "초록색으로 바꿔줘" 입력
8:05 - AI 자동 적용
8:06 - 브라우저 자동 새로고침
8:07 - 확인 완료
---
총 7분, 0번의 창 전환
```

**절감**: 60% 시간 단축, 스트레스 90% 감소

---

## 🚀 핵심 가치 제안

1. **Zero Context Switch**
   - 모든 것이 한 화면
   - 창 전환 불필요
   - 집중력 유지

2. **Visual-First Workflow**
   - 보이는 것을 클릭
   - 정확한 정보 자동 추출
   - 복붙 불필요

3. **AI-Powered Intelligence**
   - 정확한 context
   - 자동 코드 생성
   - 즉각적인 피드백

4. **Developer Joy**
   - 마법같은 경험
   - 생산성 2배 향상
   - 즐거운 개발
